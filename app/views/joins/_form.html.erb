<div class="form-wrapper new-yacht-form">
    <%= simple_form_for(join) do |f| %>
      <div class="form-content">

        <div class="form-input">
          <%= f.input :file, as: :file %>
        </div>

        <div class= "form-button">
          <%= f.button :submit, "valider", class: "form-button-text"%>
        </div>
      </div>
    <% end %>
  </div>
